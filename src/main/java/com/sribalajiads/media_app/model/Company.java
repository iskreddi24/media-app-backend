package com.sribalajiads.media_app.model;

/**
 * Represents the company to which a media asset belongs.
 * Using an Enum ensures type safety and restricts values to a known set.
 */
public enum Company {
    SBA,
    OUTSPACE, // Use a consistent format for enum values
    YUVA
}